var GHPATH = 'https://simpsons-generator.github.io/test';
var APP_PREFIX = 'simp.gen';
var VERSION = 'version_003';
var URLS = [    
  `${GHPATH}/`,
  `${GHPATH}/index.html`,
  `${GHPATH}/sw.js`,
  `${GHPATH}/manifest.webmanifest`,
  `${GHPATH}/images.png`,
  `${GHPATH}/style.css
  `${GHPATH}/script.js
]
